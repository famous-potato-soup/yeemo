const BaseGameRule = {
    MAX_PLAYER: 20,
    TIME_FOR_TURN: 3000,
}

const RoomStatus = {
    waiting: 'waiting',
    playing: 'playing',
    done: 'done'
}

export default {
    BaseGameRule,
    RoomStatus
}